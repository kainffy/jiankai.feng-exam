import { ShowSorterPipe } from './show-sorter.pipe';

describe('ShowSorterPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowSorterPipe();
    expect(pipe).toBeTruthy();
  });
});
